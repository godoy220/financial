# Regra que já está aí
node_modules/
/frontend/node_modules

# ----------------------------------------------------------------------
# SEGURANÇA CRÍTICA: Ignora todos os arquivos de variáveis de ambiente
# (como .env, .env.development, .env.test, etc.) para evitar vazamento de segredos.
# ----------------------------------------------------------------------
*.env

# NOVAS REGRAS ESSENCIAIS DE CACHE:
.DS_Store
npm-debug.log
yarn-error.log
.eslintcache
.vscode/

# Regra para ignorar caches do Vite/Next/etc dentro do frontend
/frontend/dist
/frontend/.vite
/frontend/.next